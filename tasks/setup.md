Using esbuild for bundling, vitest for testing, TypeScript, and Biome for
linting.

Output is a single JS file for drop-in use via script tag. End-users attach
the roller to clickable elements, no complex build required on their end.

- [X] initialise project with build tooling
- [X] in CI run tests and then build output file
